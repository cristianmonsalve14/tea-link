# Copie a .env.test para pruebas locales/CI.
# Los tests de integración NO deben usar la BD de Neon/producción.

INTEGRATION_DATABASE_URL="postgresql://postgres:SU_PASSWORD@localhost:5432/tea_link_test"
JWT_SECRET="test-secret-key"

# Solo en emergencia (no recomendado):
# ALLOW_PRODUCTION_INTEGRATION_TESTS=1
